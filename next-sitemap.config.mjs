// No need to import anything — just export the config object

/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://heartcrop.netlify.app', // 🔁 Change when you buy a domain
  generateRobotsTxt: true,
};

export default config;
